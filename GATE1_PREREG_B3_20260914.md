# GATE1 B-3 决策点预注册（GATE1_PREREG_B3_20260914）

> **性质**：**开跑前（before-run）预注册**，不是结果记录。本文件在 B-3 训练启动**之前**落盘，用于固定判据、口径与路线映射，杜绝"看到数据再定判据"。
> **依据**：`GATE1_FREEZE.md`（冻结主判据与更严门）、`GATE1_AMEND_20260914.md`（口径补录先例）、`专家团审查报告-可行性评估v2-20260914.md`（M1/M9）、`可行性评估报告-CGA论文整改-20260914.md` v3。
> **纪律**：本文件**不含任何新数字**。B-3 的实测结果一律写入 `GATE1_UNFREEZE_<实际日期>.md §3`，本文件不修改。`GATE1_FREEZE.md` 原文不动。

---

## 1. 为什么需要这份预注册

专家团复审发现三处会导致**判据错用**的缺陷：

1. **M1 判据口径冲突**：`arb_full300_stats.py` 原先只打印 `[gate-C1] Δ≥0.02`（**更严门**），从不打印冻结**主判据**。而 `GATE1_FREEZE.md` L58-64 明确 `≥+0.02` 属于"更严门"。**冻结数据下主判据 PASS 而 gate-C1 FAIL** —— 照脚本输出判读会做出**相反的路线选择**。
2. **口径未开跑前登记**：B-3 引入新 BASE 口径，若只在事后写入结果账本，等于"看到数据再定口径"。
3. **M9 决策矩阵缺格**：最可能落点（主判据部分通过 + 更严门全 FAIL）在原计划中**没有对应格**，结果出来时只能临时拍板。

本文件即针对以上三点的**事前**处置。

---

## 2. 判据分层（必须分层报告，不得混用）

| 层级 | 定义 | 来源 | 是否决定路线 |
|---|---|---|---|
| **主判据 `[primary-H5]`** | 高冲突 recall 的精确二项（McNemar 式）`p < 0.05` **且** `recovered − lost ≥ 3` | `GATE1_FREEZE.md §3`（冻结） | **是——唯一决定项** |
| **更严门 A `[gate-C1]`** | 高冲突 recall `Δ ≥ +0.02` 且 `n01 > n10` 且 `p < 0.05` | `GATE1_FREEZE.md L58-64`（注册为"更严门"，冻结证据 **FAIL**） | **否**，仅如实报告 |
| **更严门 B（C2）** | 全目标 `mAP50 Δ ≥ +0.01`（vs B0cmp 0.7197） | 同上（冻结证据 **FAIL**） | **否**，仅如实报告 |
| **更严门 C（C3）** | SF/AG 相对非劣性在 −1% 内 | 同上（冻结 **2/3 PASS**） | **否**，仅如实报告 |
| **统计前提** | 逐图配对 Wilcoxon + Holm（族 = 6，或按实际比较数） | `机制消融实验协议.md §5` | 否，但缺失即整项无效 |

**硬规定**：
- 决策**只认** `[primary-H5]`。
- `[gate-C1]`/C2/C3 的结果**必须在论文中如实报告**（预注册纪律第 3 条：负结果必写），但**不得**作为路线选择的依据。
- 任何"因为 C1 没过所以走路线 B"的推论**无效**——这正是 M1 要防的错。

---

## 3. 两个口径的定义与并报规则

B-3 引入的口径与冻结值**不同**，这是刻意的，但必须并报：

| 口径 | 定义 | 状态 |
|---|---|---|
| **口径 A（冻结）** | TEST = `CGA_str_s<seed>`；BASE = **共享** `B0cmp_full`（即 s42 的 20 轮 B0） | `GATE1_FREEZE.md §3` 已冻结 |
| **口径 B（新，本文件登记）** | TEST = `CGA_str_s<seed>`；BASE = **各自** `B0_e32_s<seed>`（同种子、32 轮预算匹配） | 本文件 + `GATE1_UNFREEZE §3` |

**并报规则**：
1. 两个口径的数值**必须同时呈现**，不得只报其中一个。
2. **口径 B 与口径 A 的数值不可直接比较**——两者 BASE 不同，不是同一个对照。
3. 措辞：口径 B 应描述为"**预算匹配（更公平）**"，**不得**写"更严格"。原计划把口径 B 称为"更严格"属**未经检验的预设**：BASE 换成了训练更充分的模型，Δ 大概率收缩，但这不等于"门槛更高"。
4. 口径 A 的冻结值**一字不改**；口径 B 全部作为新增数字经 `GATE1_UNFREEZE` 入账。

---

## 4. 聚合规则（事前注册）

**三种子读数在 B-3 下的实际独立性需要说明**：s123 与 s3407 的高冲突统计在冻结记录中逐位相同（18/4），经复核为**确定性结果**（高冲突子集由输入对计算故跨变体恒等；两臂全目标 recall 差恰为 1/2583 个目标，必落在高冲突集之外）⇒ **有效独立读数实为 2**。

因此：

1. **聚合判据**：`k = 三种子中通过主判据的个数`，**k ≥ 2** 方视为"主判据在预算匹配下复现"。
2. **跨种子合并检验**：除逐种子精确二项外，另报告三种子的**合并检验**（按图像分层的 Mantel–Haenszel 或 Fisher 合并，实现方式在 `GATE1_UNFREEZE` 中写明），避免"逐种子都勉强过"堆叠成假阳性。
3. **"三种子方向一致"不是统计判据**：在零假设下，三种子随机同向的概率为 (1/2)² = **25%**，远高于 0.05。它只能作为**描述性**证据，**不得单独作为路线依据**。
4. **多重比较**：跨种子若做 6 指标 × 3 种子检验，须给 Holm 校正后的 p；未校正的逐种子 p 仅作描述。

---

## 5. 完整决策映射表（含原计划缺失格）

**输入**：`k` = 通过主判据的种子数；`dir` = 高冲突 recall Δ 的符号一致性；`qual` = 质量侧 6 指标池化 Wilcoxon/Holm 方向是否保持（EN/MI/QABF/VIF 正、SF/AG 负）。

| # | k | dir | qual | 判读 | 路线 | 后续动作 |
|---|---|---|---|---|---|---|
| **R1** | 3 | 全为正 | 保持 | 主判据三种子全过 | **A** | 维持定位；B-5 主表三种子并列；继续 P2/P3；正文仍须如实写 C1/C2 未过 |
| **R2** | 2 | 全为正 | 保持 | **主判据部分复现**（← 原计划缺失格，且是最可能落点） | **A−（默认）** | **维持定位但强约束措辞**：只能写"主判据在 2/3 种子复现（预算匹配口径），更严门全部未过"；**不得**写"三种子稳健"；贡献声明降半档；**是否降为路线 B 由用户裁定** |
| **R3** | 2 | 全为正 | **不保持** | 机制有增益但质量代价扩大 | **A−** | 同上，且 Limitations 第 1 条（结构代价）须按新口径重写 |
| **R4** | 1 | — | — | 不足以支撑机制主张 | **B** | 重定位为"受控证伪 + 预注册协议"；标题改中性；全链改写约 2 天 |
| **R5** | 0 | — | — | 主结论被证伪 | **B** | 同上；并在正文明确写出"预算匹配后机制增益不成立" |
| **R6** | 任意 k | **存在负向种子** | — | 机制不稳定（存在异号） | **B** | 无论 k 多大均走 B：方向不一致意味着效应不稳健，不能作为机制证据 |
| **R7** | 任意 k | — | — | **coverage 不对称**（`[integrity]` FAIL） | **暂不判定** | 先修数据（补齐预测或说明缺失原因）再判定；**不得**在残缺数据上选路线（M3） |
| **R8** | 任意 k | — | — | 判据/口径有一项缺失或不可复算 | **暂不判定** | 补齐后重跑；不得用近似替代 |

**注**：
- R2/R3 是**最可能落点**（专家团分析：CGA 的 s123/s3407 best 落在 ep11/12，实质是 11–12 轮模型；而 `B0_e32` 跑满 32 个干净 epoch ⇒ BASE 变强、Δ 大概率收缩）。
- **R2/R3 需要用户裁定**，不在执行会话自行决定。裁定结果与判据数值一并写入 `GATE1_UNFREEZE §3`。
- 表中任何路线**都不允许**在事后修改本文件——若判据本身需要改，须另起 `GATE1_PREREG_*_V2` 并在其中说明改动理由与时点。

---

## 6. 无效与中止条件（事前注册）

出现下列任一情形，B-3 结果**不得**用于路线判定，须先修复再重跑：

1. `[integrity]` 报出预测覆盖不对称（`miss_b ≠ miss_t`）。
2. 任一臂的融合图或检测标签数量低于该臂历史水平（生成或检测中途失败）。判定阈值：融合图必须 = 300；检测标签数须 ≥ 该臂冻结记录值。
3. 32 轮 B0 出现 val NaN（冻结记录称 B0 系 20 轮全程无 NaN）。**这不是中止条件，而是必须如实入账的新发现**：若出现，按 `GATE1_FREEZE §5` 口径记录 NaN 起始 epoch 与 best epoch，并把"预算匹配后基线亦不稳定"写入 Limitations。
4. 训练因崩溃中断且 `_last.pt` 缺失导致无法续训（M5 已修，续训不再静默回退）。

---

## 7. 留档要求

1. 判定用的原始 stdout 完整保存（建议 `logs/after_seeds_v2.log` 追加段即可）。
2. `GATE1_UNFREEZE_<实际日期>.md §3` 必须包含：三种子 best epoch、val NaN 情况、**口径 A 与口径 B 两套数值**、`[primary-H5]` 逐种子结果、质量侧池化检验、`k` 值与 `dir`/`qual` 判定、**所选路线及依据**。
3. 若走路线 B，另附重定位方案要点（标题、贡献声明改法）。
4. 本文件在判定后**不改**，仅在新文件中引用。

---

*预注册人：墨白（AI 助手），2026-09-14。落盘时点早于 B-3 训练启动。*
