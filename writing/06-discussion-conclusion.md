# 6 Discussion — 7 Limitations — 8 Conclusion (Draft)

> Status: DRAFT for gate-2. All numbers verbatim from `GATE1_FREEZE.md` (2026-09-13 freeze), annotated per block with `<!-- src: ... -->`; positioning claims trace to `writing/02-related-work.md` §6 and `writing/01-intro-mechanism.md` (K7). Negative results reported per the writing-discipline contract (`00-PAPER-CONFIG.md`): H1 falsification, B2 distillation failure, late-stage val NaN, C1/C2/C3 narrow misses, SF/AG structural costs. Interpretive sentences are flagged as such; no new claims beyond the frozen evidence. Target journal: The Visual Computer.

## 6 Discussion

This section reads the frozen evidence in three registers: what it supports, what it does not, and where the method sits. The paper's contribution is a verified mechanism with an honest evidence profile; conflating the registers would damage exactly that.

### 6.1 What the evidence supports

The pre-registered question of this study was narrow by design: at pixels where the infrared and visible modalities disagree, does an explicit image-space per-pixel commitment toward the locally salient modality recover what blending loses? Three frozen observations support an affirmative answer at the level of mechanism.

First, the learned gate is genuinely engaged rather than latent. The zero-initialized global gate scale saturates after training at tanh(gate_scale) = 0.9998 <!-- src: GATE1_FREEZE §4 -->, which excludes the self-falsification scenario in which the gate stays at zero and any measured difference originates elsewhere in the pipeline. Second, the intervention is selective rather than global: on the six highest-learning conflict samples, the mean absolute CGA–baseline difference lies in 0.0125–0.0170 with difference-map peaks up to 0.594 <!-- src: GATE1_FREEZE §4 -->, and the modifications concentrate in conflict regions — exactly where the design intends them to act. Third, the direction of effect is stable across seeds. All six quality metrics are sign-consistent on 3/3 seeds (EN, MI, QABF, VIF positive; SF, AG negative) <!-- src: GATE1_FREEZE §2 -->, and the H5 primary criterion replicates on every seed: high-conflict recall gains of +0.0194, +0.0159 and +0.0159 (exact binomial p = 4.88e-04, 4.34e-03 and 4.34e-03) with recovered-to-lost object ratios of 20:3, 18:4 and 18:4 <!-- src: GATE1_FREEZE §3 (H5) -->. Under the pre-registered decision rule — p < 0.05 and recovered − lost ≥ 3 — H5 is met on all three seeds; seed-level significance tests were omitted by the same pre-registration, since with n = 3 the minimum attainable p is 0.25 <!-- src: GATE1_FREEZE §2 -->. The direction was first indicated by a training-free pre-check, in which committing toward the max-response modality in conflict regions recovers objects that blending loses; the learned gate confirms it under the registered criterion (cf. Introduction).

The profile of the quality metrics is coherent with this mechanism. Pooled per-image mean differences are positive for MI, QABF, VIF and EN, each with 3+/0− seed-direction consistency <!-- src: GATE1_FREEZE §2 -->; in the main comparison CGA attains the best QABF of the table (0.5449) and statistically ties the official weights on VIF (−0.0006) <!-- src: GATE1_FREEZE §1 -->. Commitment at conflict pixels improves cross-modal information transfer and perceptual consistency rather than high-frequency gradient energy — the two structural metrics that pay for it are treated in §6.2. The F3 stress axis points the same way: cross-scale drift-AUC falls from 0.8088 to 0.7839 (p = 1.41e-05), with significantly lower drift on four of five resolutions <!-- src: GATE1_FREEZE §5 (F3) -->. Committed outputs remain more consistent under rescaling — supporting, though not by itself proving, the reading that arbitration rather than averaging carries structure across scales.

### 6.2 What the evidence does not support

The freeze is equally explicit about the boundaries, and we report them in the same voice as the gains.

First, the magnitude of the primary detection gain is statistically significant but below the stricter pre-registered gate thresholds. Gate-C1 (high-conflict recall gain ≥ +0.02) fails on all three seeds (+0.0194, +0.0159, +0.0159), and gate-C2 (mAP50 gain ≥ +0.01 over the 0.7197 baseline) fails likewise (+0.0085, +0.0028, +0.0035) <!-- src: GATE1_FREEZE §3 (C1/C2) -->. The frozen grade verdict retains the main narrative (mechanism significant, three-seed robust) while fixing the wording at "below the stricter pre-registered gate thresholds"; at no point do we claim the stricter gates were passed <!-- src: GATE1_FREEZE §3 (档位判定) -->.

Second, the structural cost is real, significant, and mechanism-inherent. SF and AG pool to −0.0713 and −0.1295 with 0+/3− seed-direction consistency (Holm-corrected p = 1.09e-122 and 4.35e-147) <!-- src: GATE1_FREEZE §2 -->, and RGB_SSIM (−0.0093) and Colorfulness (−0.0043) move in the same direction <!-- src: GATE1_FREEZE §5 (K6) -->. A mechanism-consistent reading is that committing to one modality removes the averaging that partially inflates gradient-energy metrics at boundaries; we report this as the expected price of arbitration, not training variance, while flagging the reading itself as interpretive. The pre-registered non-inferiority gate C3 passes on two of three seeds (s123: SF −0.39%, AG −0.60%; s3407: SF −0.21%, AG −0.36%) with s42 the borderline AG exception at exactly −1.00% <!-- src: GATE1_FREEZE §3 (C3) -->.

Third, the evidence mainline is single-dataset: every frozen number derives from M3FD <!-- src: GATE1_FREEZE §1–§5 -->, so no cross-dataset generalization claim is made. Fourth, the reliability-map distillation arm (B2) is a registered negative result — validation NaN from the first epoch, no best checkpoint ever saved <!-- src: GATE1_FREEZE §5 (B2) --> — reported as a failure record, not rehabilitated.

### 6.3 Positioning

The comparison with the closest conflict-aware methods is defined on mechanism axes, not on gain magnitude. ARC3Fusion [Sensors 2026] coordinates consensus–complementarity–conflict in feature space through per-position three-branch Softmax weights that sum to one, and GatedFusion-Net [Information Fusion 2026] learns per-pixel Sigmoid confidence masks applied additively; both are continuous, feature-space, soft coordinators <!-- positioning per writing/02-related-work.md §6 -->. CGA differs on both axes at once: image space rather than feature space, and a hard commitment rather than a soft weight, triggered only where gradient-orientation or polarity conflict is detected. This positional distinction is independent of the gain magnitude — a small gain weakens the practical-value claim, not the axis distinction — and it is what survives the grade verdict intact.

The commitment philosophy itself has a long lineage. The classical select/choose fusion rules [Burt & Kolczynski 1993; Zhang & Blum 1999; Piella 2003] formalized choosing the salient source where sources disagree, and the learning-based multi-focus decision-map line [e.g., Ma et al., Neurocomputing 2021] still makes per-pixel binary choices — but driven by defocus within same-modality pairs <!-- positioning per writing/02-related-work.md §6 -->. CGA inherits the commitment idea and moves it to cross-modal conflict: the trigger is inter-modal gradient conflict, the commitment is learned end-to-end, and it is applied through a zero-initialized gate that preserves official checkpoint compatibility at load time.

Finally, the positioning is deliberately that of a mechanism verification, not a comprehensive superiority claim. In the frozen main table EMMA leads SF and AG, and Diff-IF leads MI <!-- src: GATE1_FREEZE §1 -->; CGA's leading metric is QABF, with VIF a statistical tie against the official weights. S4Fusion serves as the testbed because it is the representative open scan-and-blend selective state-space backbone whose public weights make the falsification controlled — not because it is assumed to be the strongest backbone <!-- consistent with writing/01-intro-mechanism.md (K7) -->.

## 7 Limitations

The following limitations are reported exactly as frozen; none is mitigated by post-hoc argument.

1. **Structural cost.** CGA trades high-frequency structure for arbitration: pooled SF/AG differences are −0.0713/−0.1295 with 0+/3− seed consistency <!-- src: GATE1_FREEZE §2 -->. The pre-registered C3 non-inferiority gate passes on 2/3 seeds; s42 is the AG borderline exception (−1.00%, exactly at threshold) <!-- src: GATE1_FREEZE §3 (C3) -->.
2. **Narrow miss on the stricter gates.** C1 and C2 fail on all three seeds <!-- src: GATE1_FREEZE §3 (C1/C2) -->. The wording is fixed at "statistically significant but below the stricter pre-registered gate thresholds" and never claims the stricter gates were passed.
3. **B2 distillation failure and late-stage val NaN.** The reliability-map distillation arm never produced a usable checkpoint: validation NaN from epoch 1, NaN on all subsequent epochs, best never saved; only an unusable last checkpoint remains, and per pre-registration it was neither re-tuned nor re-run <!-- src: GATE1_FREEZE §5 (B2) -->. Separately, CGA-line seeds exhibit late-stage val NaN (s123 from epoch 12, s3407 from epoch 13 <!-- A-5 2026-09-14: corrected 14 -> 13; GATE1_AMEND_20260914.md §3 explicitly supersedes the GATE1_FREEZE §5 value -->; best checkpoints 0.3090/0.3063 saved before onset), while the two B0-line seeds retrained in the same replication queue (s123, s3407) run all 20 epochs without NaN <!-- src: GATE1_FREEZE §5 (NaN 现象) -->. The seed-42 CGA run whose best checkpoint feeds the main table selected its best at epoch 26 — beyond the 20-epoch B0 schedule — so the training length differs across arms; adopters who retrain the recipe on their own data face an unresolved divergence risk, and the frozen best-validation selection rule is the only safeguard in this round. A commit-loss divergence hypothesis is recorded and left for future work; this round does not act on it.
4. **Single-dataset mainline.** The frozen evidence is M3FD-only; out-of-distribution datasets were not part of this freeze round, and no generalization claim is made. The downstream-detection endpoint additionally uses a single fixed detector (the DCEvo detection branch) across all arms; within-arm comparisons are internally valid, but the absolute recall levels are detector-specific. A residual alternative explanation is therefore not fully closed: commitment shifts local contrast at conflict pixels, and part of the high-conflict recall gain could, in principle, reflect alignment of those shifts with this detector's preferences rather than improved fusion fidelity. The all-object mAP50 gains (positive on all three seeds, outside the conflict-subset definition) and the mechanism-external gate criteria are the evidence against this reading, but a second detector would be needed to close it definitively.
5. **Per-pixel independent gating.** The commitment decision is made independently at each pixel from local gradient conflict, with no spatial regularization on the commitment field. The observed concentration of modifications in conflict regions is an empirical property of the trained gate <!-- src: GATE1_FREEZE §4 -->, not an architectural guarantee; spatially regularized commitment fields remain untested.
6. **Registration sensitivity.** The conflict signal κ is computed from aligned source pairs; a 1–2 pixel misalignment can turn a shared edge into an apparent gradient-orientation conflict and trigger commitment toward one modality. The frozen experiments use the registered M3FD pairs, so no robustness claim is made for imperfectly registered inputs, and deployments with residual registration error should verify alignment upstream of the gate.
7. **Budget asymmetry between arms.** The CGA arm follows the registered 32-epoch extended schedule while the B0 baseline follows the unified 20-epoch schedule; no 32-epoch B0 control exists in the frozen record, so the CGA−B0 deltas bundle the gate, the commit-weighted loss, the gate learning rate, and the extended schedule. The asymmetry is not uniform across seeds: the best-validation checkpoints of the two NaN-affected CGA runs fall at epoch 11 (s123) and epoch 12 (s3407), both *inside* the 20-epoch B0 budget, so for those two seeds the arms are matched in effective training length; only the seed-42 run, whose row feeds the main table, selected its best at epoch 26 and therefore exceeds the B0 schedule. A budget-matched 32-epoch B0 control for all three seeds remains the cleanest way to isolate the gate itself and is left to future work.
<!-- A-5 2026-09-14: explicit best epochs (11/12) added and items 4/6/7 aligned with main.tex §Limitations, which had already grown to 7 items while this source file still carried 5 and a stale NaN epoch (14 instead of 13). -->

## 8 Conclusion

This study set out to locate the residual failure of scan-and-blend selective state-space fusion at modal boundaries. The controlled falsification returned a clear negative: feature-space modulation of the state dynamics, motivated by state-capacity dilution, significantly degrades rather than improves the structural metrics SF, AG and QABF under a same-budget protocol fine-tuned from identical public weights <!-- src: GATE1_FREEZE §3 (H1) -->. The bottleneck of boundary fusion in this operator class is not the state; it is the absence of an arbitration decision where the modalities conflict.

We introduced Conflict-Gated Arbitration as the corresponding mechanism: an image-space, conflict-triggered, per-pixel commitment toward the locally salient modality, carried by a zero-initialized gate that preserves the official checkpoint exactly at load time and saturates when trained <!-- src: GATE1_FREEZE §4 -->. Across three seeds the primary pre-registered criterion replicates, the quality-metric directions are fully consistent, and the modifications concentrate where the modalities disagree <!-- src: GATE1_FREEZE §2–§4 -->.

The evidence boundary is reported with the same discipline: the detection gains are statistically significant but below the stricter pre-registered gate thresholds; significant SF/AG structural costs accompany them; the mainline is single-dataset; and the distillation arm is a registered failure <!-- src: GATE1_FREEZE §2, §3, §5 -->. Within these boundaries, the contribution is a verified arbitration mechanism — image-space per-pixel commitment at cross-modal conflict pixels — together with the pre-registered, same-budget protocol that made both the falsification and the verification interpretable.

Whether spatially regularized commitment fields, cross-dataset validation, or a stabilized commit loss extend these gains remains open; we list them as questions, not promises.

---

## Notes for gate-2 integration

**数字溯源**：正文（不含本 Notes 与状态行）共 26 处 `src: GATE1_FREEZE` 数字注释（部分块多源标注），分布于 §1 主表 / §2 种子 / §3 H1+H5+C1/C2/C3+档位 / §4 机制可视化 / §5 F3+K6+B2+NaN；另有 2 处定位注释指向 02-related-work §6（ARC3Fusion/GatedFusion-Net 刻画与 select/choose 谱系，非数字）、1 处指向 01-intro K7（测试床理由）。全部数字逐字核对冻结清单，无手填；摘要层面不放任何涨幅数字（见 00-abstract.md）。

**与 01 章 claim-evidence map 的呼应闭环**：
- "State-modulated BRSS fails to improve structural metrics" 行 → §8 第一段(H1 证伪复述,口径=GATE1 §3 的 SF/AG/QABF 显著负向);
- "CGA improves downstream detection on high-conflict objects" 行 → §6.1 第三观察 + H5 三种子复现(20:3 / 18:4 / 18:4 与 p 值全部同源);
- "Zero-initialized gate preserves official weights" 行 → §6.1 第一观察(0.9998 饱和 = 排除"门未打开"的自我证伪)+ §6.3 零初始化兼容句;
- "Blending cancels conflict structure; committing recovers detection"(training-free pre-check)→ §6.1 末句以 "cf. Introduction" 回指,不重述 A3_max 数字(其 aggregate-mAP 限定口径由 01 章自行承担);
- K7 三理由 → §6.3 末段("representative open scan-and-blend backbone... not because it is assumed to be the strongest")。

**潜在措辞冲突(需 gate-2 终检)**：
1. **drift 双臂勿混**：01 章 claim-evidence map 的 drift 行是 H1/BRSS 臂(B0=S=0.7895,0/5 resolutions,来自 eval_c256.log,不在 GATE1 冻结清单内);§6.1 引的是 CGA 臂 F3(0.8088→0.7839,4/5,SUPPORTED,GATE1 §5)。终稿两处 drift 数字并存时必须明确标注所属臂,防审稿人视为矛盾。
2. **C3 口径代际差**:01-intro §Assumptions 已更新为 2/3 种子通过 + s42 边界例外,但其 §4 outline 段仍残留单种子时期 "AG structural non-inferiority fails at exactly −1.00%" 的旧口径表述;06 章按 GATE1 §3 冻结口径(2/3 PASS + 边界例外)书写,gate-2 重写 01 章时须同步清除旧表述。
3. **MI 两个不同量**:01 章 H1 消融的 MI +0.0213(eval_H1.log)与 GATE1 §2 CGA 池化 MI +0.7066 是不同臂、不同量级;06 章对 CGA 池化 MI 仅作定性引用("positive with 3+/0−"),避免与 01 章 MI 数字并置产生混淆。
4. **边界句式双版本**:正文用合同固定句 "statistically significant **but** below the stricter pre-registered gate thresholds"(00-PAPER-CONFIG 纪律 2);摘要按任务书用 "yet" 变体。gate-2 终检时确认两版本并存是有意的(摘要变体为任务书指定),或统一为 "but"。
5. "VIF 全表第一 = 官方权重" 的表述:§6.1 写作 "statistically ties the official weights on VIF (−0.0006)",与 GATE1 "统计上平手" 口径一致;终稿勿写成 "CGA achieves the best VIF"(那是错误归属)。
