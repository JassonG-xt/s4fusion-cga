# 机制消融 + 状态响应可视化 实验协议

> 服务目标:为决策 D1(核心命题=机制为主,高分辨率为压力轴)提供可证伪证据,正面回应 K1(创新是否为主干内部机制)与 K3(高分辨率缺陷是否定位到机制层)。
> 关联文件:`顶层重构设计计划.md`(第 5、8、9 节)、`code/modules/fusion.py`、`code/modules/boundary.py`、`code/train_brss.py`、`code/evaluate_brss.py`。
> 诚信约束:本协议中所有指标为**待产出**;结果冻结(闸门-1)前不得当作已完成。凡标注「需最小钩子」者为当前 CLI 不可达、须先补代码入口的项,不得直接写进复现命令假装已存在。

---

## 0. 一句话设计

用 `boundary_mode ∈ {none, residual, state, full}` 把**主干内部的 B/C/Δ 状态调制**(`state`,`fusion.py` 264–281)与**主干外部的空间残差/下采样分支**(`residual` 357–364;旧 MS-Global `global_scales`)在**同一微调预算、同一权重起点**下拆开,证明增益来自算子内部机制,而非外挂容量或额外监督。

---

## 1. 可证伪假设(先注册,后实验)

| 假设 | 陈述 | 通过判据 | 不通过的后果 |
|---|---|---|---|
| **H1 机制有效** | `state`(内部调制,外部分支关闭)相对 `none` 在结构类指标上显著提升 | ≥2 个结构指标(SF/AG/QABF/EOR)成对 Wilcoxon,Holm 校正后 p<0.05,且新增参数≈0 | **D1 不成立** → 命题回退,不硬写"机制为主" |
| **H2 内部优于外部** | `state` 在结构指标上 ≥ 旧 `MS-Global`(none+scales),且参数远少 | state 参数 ≪ MS-Global,且结构指标不劣 | 内部机制叙事薄弱,需重新论证 K1 |
| **H3 可靠性内容有意义** | 学习到的可靠性图优于均匀/打乱可靠性 | 学习 vs 均匀、学习 vs 打乱,Wilcoxon p<0.05 | 可靠性沦为"最大梯度选择器",退化为安慰剂(`boundary.py` 注释已预警) |
| **H4 机制≠监督** | 关闭边界监督损失后,`state` 仍保留多数增益 | 关损失版保留 H1 增益的 ≥60% | 增益来自辅助监督而非架构,须改叙事 |
| **H5 CGA 增益可检出**(2026-09-09 登记,见 §10 弱增益预案;先注册后实验) | 强化训练后的学习版 CGA(冲突加权损失+更多 epoch)相对同预算基线在高冲突目标的检测 recall 上产生可检出增益 | M3FD 检测:高冲突子集 recall 增益 McNemar p<0.05 **且** 恢复目标数−丢失目标数 ≥3 | 触发 §10 预案第 2/3 档:CGA 降级为 registered negative result / 边界探测,与 H1 证伪并列;标题/摘要去除机制增益承诺,不硬写 |

> H1–H5 是 D1 的**闸门**。任一核心假设不通过,先改叙事,不改数据。H5 为 CGA 叙事(引言已转向)的对应闸门:H1 管 BRSS 机制、H5 管 CGA 决策——两线独立读数,任一失败各自降级,不互相兜底。

---

## 2. 变量分解(映射真实代码开关)

| 因子 | 取值 | 代码入口 | 语义 |
|---|---|---|---|
| 边界模式 | none / residual / state / full | `--boundary-mode`(train/eval) | none=基线;state=内部 B/C/Δ 调制;residual=外部空间残差;full=两者 |
| 旧全局分支 | none / `4,8` | `--fusion-scales` | 旧 MS-Global 下采样-扫描-上采样外挂 |
| 门控 | scalar / dynamic | `--gate-type` | 仅当 fusion-scales≠none 时有意义 |
| 边界监督损失 | 开 / 关 | `--w-boundary --w-reliability --w-scale` 置 0 | H4 正交控制 |
| 随机种子 | 42 / 123 / 3407 | `--seed`(已有 `_set_seed`) | 稳健性 |

**Step 0 钩子状态:三个钩子已实现并通过 WSL 回归测试(`tests/test_brss.py` 8 passed)**:
- (a) `--global-op {scan,pool1x1,dw3x3}`(H2 外部算子替身)→ **已实现**。加于 `train_brss.py` / `evaluate_brss.py`,`build_model` 经 `getattr(args,"global_op","scan")` 透传 `fusion_global_op`(向后兼容)。`fusion.py:96` 的硬编码路径已由 CLI 打通。
- (b) `--reliability-override {none,uniform,shuffle}`(H3 证伪)→ **已实现**。`modules/boundary.py` 的 BRE 新增 `reliability_override` 属性 + `_apply_override()`:`uniform`=逐位 1/3;`shuffle`=固定置换,只破坏空间对应、保留每通道多重集与"通道和=1";`none` 非破坏、可复原。`evaluate_brss._apply_reliability_override()` 遍历全部 BRE 设值,`boundary-mode=none` 时给告警不崩。
- (c) `--dump-boundary <dir>`(可视化转储)→ **已实现**。`_dump_learned_scales()` 出 `learned_scales.csv`(每 block `tanh(state/residual_scale)`,即 F5);`_dump_boundary_maps()` 每图存 `.npz`(weights ir/vi/shared、boundary、confidence、ir/vi_edge、conflict,供 F1–F4)。用独立 native aux forward,不干扰指标用的 tiled 推理,故配 `--limit`;高分辨率原图可能 OOM,属已知取舍。

> 诚信边界:上述为**功能验证**(旗标解析 + helper 单测 + override 数学性质)在 WSL CPU + scan-fallback 下真跑通;H1–H4 的**性能数字仍待产出**,须 GPU 训练/评测并贴回真实 stdout 才闭环。

---

## 3. 消融单元格(全部从同一 `model.pkl` 微调,同预算)

| 单元 | boundary-mode | fusion-scales | 边界损失 | 作用 |
|---|---|---|---|---|
| B0 | none | none | — | 同预算重训基线(避免"多训 vs 机制"混淆) |
| G  | none | 4,8 (dynamic) | — | **旧 MS-Global**(被拒设计),H2 对照 |
| R  | residual | none | 开 | 仅外部空间残差 |
| **S** | **state** | none | 开 | **核心:仅内部 B/C/Δ 调制** |
| F  | full | none | 开 | 内部+外部残差 |
| F+G | full | 4,8 (dynamic) | 开 | 完整顶配 |
| S⁻ | state | none | **关**(w-boundary/reliability/scale=0) | H4 机制 vs 监督 |

推理期附加(不训练,基于 S 权重,需钩子 b):
- S/unif:S 权重 + 均匀可靠性;S/shuf:S 权重 + 打乱可靠性 → H3。

**关键对照读法**:
- `S − B0` = 内部机制净增益(H1)。
- `S vs G` = 内部机制 vs 外部分支,看参数-性能比(H2)。
- `R vs S` = 外部残差 vs 内部调制,谁贡献主体。
- `S vs S⁻` = 架构 vs 监督(H4)。
- `S vs S/unif vs S/shuf` = 可靠性内容是否有意义(H3)。

---

## 4. 混淆控制(方法学审稿视角预清扫)

| 潜在质疑 | 控制手段 |
|---|---|
| "增益来自多训练一轮" | 所有单元同起点(`model.pkl`)、同预算(见 §6),含 B0 重训基线 |
| "增益来自新增参数容量" | 报告每单元 Δparams;S 仅 +5 参数(state_scale 3 + residual_scale 2 之 state 部分),用参数-性能比论证 |
| "增益来自额外监督而非架构" | S⁻ 关边界损失(H4) |
| "可靠性只是最大梯度选择器" | S/unif、S/shuf 覆盖(H3),`boundary.py` 已内建结构先验+学习残差以防退化 |
| "只是低频上下文,scan 无必要" | (可选)`--global-op pool1x1/dw3x3` 替身对照(钩子 a) |
| "单种子偶然" | 3 种子 + 成对检验 + 自助置信区间(§5) |

---

## 5. 统计协议

- **种子**:42 / 123 / 3407,每单元 3 次;报告 mean±std。
- **成对检验**:逐图配对 Wilcoxon 符号秩(同测试集同图对),对每个指标做 `S vs B0`、`S vs G`、`S vs S⁻`、`S vs S/unif`、`S vs S/shuf`。
- **效应量**:中位数 Δ + 95% bootstrap CI(逐图,B≥1000)。
- **多重比较**:同一族对比内 Holm 校正。
- **效率**:每单元 Δparams、FLOPs、单图推理延时(defuse 容量论)。
- **指标集**:核心 EN/SF/AG/MI/QABF(`evaluate_brss.py`);扩展 VIF/EOR/SSIM 用 `eval_metrics_extended.py`(先确认覆盖面)。MI/VIF 若下降,按 K5 单独做显著性+失败案例,不藏。

---

## 6. 统一实验条件(固定不变量)

- 起点权重:`../../S4Fusion-main/model/model.pkl`(全单元同一)。
- 训练:`--crop-size 128 --batch-size 1 --accumulation-steps 8 --samples-per-epoch 2048 --epochs 20 --lr 2e-5 --amp`。
- 报告样本数与优化步数(README 要求)。
- 评测:同一 `--tile-size` 与 `--tile-overlap`(全单元一致),评测器逐行记录推理模式。
- 数据:先用 M3FD 测试集出机制信号;HR 压力轴见 §7-F3。

---

## 7. 状态响应可视化套件(答 K1+K3,基于真实张量)

来源:`BoundaryReliabilityOutput`(weights ir/vi/shared、boundary、confidence、ir_edge、vi_edge、内部 conflict)、`tanh(boundary_state_scale[3])`、`tanh(boundary_residual_scale[2])`;增益场由 `fusion.py` 269–273 公式复算(ir_gain/vi_gain/shared_gain/delta_ir/delta_vi)。

| 图 | 内容 | 回应 |
|---|---|---|
| F1 可靠性三元图 | ir/vi/shared 权重 + conflict 图,叠加模态边界 | K1:调制集中在模态边界 |
| F2 状态增益场 | B/C-gain、Δ-gain 空间图 | K1:展示 scan 被"如何"重塑,非黑箱 |
| **F3 多分辨率退化曲线** | 同场景 {256,512,1024,native} 下,`none` vs `state` 的全局一致性度量 + 可靠性跨分辨率一致性 | **K3:把"指标波动"升级为机制层退化曲线,连接 HR 压力轴** |
| F4 证伪面板 | learned vs uniform vs shuffle 可靠性的输出/指标 | H3:可靠性内容有意义 |
| F5 学习标度 | 各 block 的 `tanh(state_scale/residual_scale)` 幅值 | 证明机制训练后"打开"(非零);若≈0 即自证伪 |

> F3 是连接"机制"与"高分辨率压力轴"的关键图,是 D1 叙事的承重图。

---

## 8. 产出物 → 审稿点映射

| 产出 | 关闭的审稿点 |
|---|---|
| 表1 消融主表(B0/G/R/S/F/F+G,指标+Δparams+延时) | K1、K7 |
| 表2 H3/H4 控制(S vs S⁻ / S/unif / S/shuf) | K1 深层、可靠性可信度 |
| 表3 统计显著性(Wilcoxon p、Holm、CI) | K5(方法级预演) |
| 图 F1–F5 | K1、K3 |

---

## 9. 执行顺序(先钩子 → 后跑数,分阶段省算力)

**Step 0(代码)✅ 已完成**:钩子 (a) `--global-op`、(b) `--reliability-override`、(c) `--dump-boundary` 均已实现并接线;`S4FUSION_ALLOW_SCAN_FALLBACK=1 pytest tests/test_brss.py -q` = **8 passed**(原 5 + 新增 3),`--help` 三旗标已注册。下一步无需再动 Step 0。

**Step 1(seed=42 全网格先过闸门)**:先只用单种子跑 B0/G/R/S/F/F+G/S⁻,验 H1/H2/H4 是否有信号。**若 H1 无信号 → 立即停,回到命题决策,别烧 3 种子。**

**Step 2(通过 H1 后补种子)**:对通过的核心单元补 123/3407,出统计表。

**Step 3(可视化)**:基于 S 权重跑 §7,重点出 F3、F4。

参考训练命令(核心单元 S,seed=42;其余单元改 `--boundary-mode/--fusion-scales/--seed`,S⁻ 追加 `--w-boundary 0 --w-reliability 0 --w-scale 0`):

```bash
cd S4Fusion-main-Innovation-2026/code
.venv-brss/bin/python train_brss.py \
  --data-root ../dataset \
  --train-manifest ../dataset/manifests/train_all.csv \
  --val-manifest ../dataset/manifests/val_all.csv \
  --checkpoint ../../S4Fusion-main/model/model.pkl \
  --boundary-mode state --fusion-scales none \
  --crop-size 128 --batch-size 1 --accumulation-steps 8 \
  --samples-per-epoch 2048 --epochs 20 --lr 2e-5 --seed 42 --amp \
  --save-path checkpoints/abl_state_s42.pt
```

评测(同 tile/overlap;B0 用 `--boundary-mode none --fusion-scales none --gate-type scalar`):

```bash
.venv-brss/bin/python evaluate_brss.py \
  --data-root ../dataset --manifest ../dataset/manifests/m3fd_test.csv \
  --checkpoint checkpoints/abl_state_s42.pt \
  --boundary-mode state --fusion-scales none \
  --output-dir results/abl/state_s42 --metrics-csv results/abl/state_s42.csv \
  --tile-size 507 --tile-overlap 64 --amp --save-rgb
```

> 闭环红线:上述命令在 WSL 不能跑 Windows GPU 时,须由你在 Windows/Linux GPU 环境运行并贴回真实 stdout(指标行 + 参数/延时),方可声明该单元完成。禁止以"预期 +N" 当交付。

---

## 10. H5 弱增益预案(2026-09-09 登记,先注册后实验;依据 `CGA重投叙事全面研究报告-20260908.md` §7a)

> 背景:K5 策略(Wilcoxon+Holm 黄金级严格检验)与 CGA 当前弱证据(学习版 +0.004 高冲突 recall,McNemar p=1.00,1 恢复 vs 0 丢失)存在交互风险——**若增益边际,最严格的尺子量出的就是自己最短处**。本预案在跑实验**之前**登记,与 H1–H4 同纪律:先注册判据,后看数据,防"结果导向改叙事"。

**当前基线读数(冻结事实,不得改写)**:`eval_cga.log` 2026-07-23 —— +0.005 recall(全体)、+0.004(高冲突子集)、McNemar p=1.00。该读数不满足 H5 通过判据,强化训练(冲突加权损失 + 更多 epoch,命令待跑)是对 H5 的正式重测。

| 闸门-1 读数档位 | 判据 | 叙事处置(预先登记,不可事后更改) |
|---|---|---|
| **A 档:显著** | H5 通过(McNemar p<0.05 且恢复−丢失 ≥3) | 主叙事不变:CGA 机制贡献 + 显著性框架正面呈现,论文照常写 |
| **B 档:边际** | p≥0.05 或恢复/丢失接近持平 | CGA 降级为 **registered negative result**,与 H1 证伪并列;论文主张收窄为 (a) 受控证伪协议的方法学贡献 + (b) training-free 预检的 decision-level lower bound(A3_max 方向证据,该证据独立于训练结果);学习版 CGA 作为"边界探测"诚实报告;标题/摘要去除机制增益承诺 |
| **C 档:负向** | 增益为负 | 同 B 档,并触发命题回退评审(`顶层重构设计计划.md` 风险登记第 1 条);考虑转向 20260824 报告 B4 方法学 short paper(SSM 融合受控证伪协议) |

**配套纪律(与档位无关,一律适用)**:
1. 与 ARC3Fusion/GatedFusion-Net 的区分是**空间(图像 vs 特征)与承诺形式(硬 vs 软)**,不是增益大小——弱增益削弱的是实用价值主张,不是定位差异;Related Work 切割句不因档位改变。
2. 相关工作中"继承 Burt–Zhang–Piella 三十年选择式规则谱系"与"IVIF 无先行工作"两套话术分区排布:谱系句在规则级背景,负检索句在"IVIF × 图像空间 × 硬承诺"合取限定下,避免自相矛盾。
3. 负检索断言(20260908 报告 §3.3 修订版)措辞在投稿前须再复验一轮,复验日期写入脚注/回复信。
4. 本预案登记后,H5 读数以**强化训练后的正式重测**为准;禁止用旧读数或部分训练的中间读数先行宣布档位。
